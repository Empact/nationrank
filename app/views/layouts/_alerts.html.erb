<% {'info' => notice, 'error' => alert, 'success' => flash[:success]}.each do |type, message| %>
  <% if message %>
    <div class="alert alert-<%= type %>">
      <a class="close" data-dismiss="alert">×</a>
      <%= icon(type) %> <%= message %>
    </div>
  <% end %>
<% end %>
